import { HigherHiglightsDirective } from './higher-higlights.directive';

describe('HigherHiglightsDirective', () => {
  it('should create an instance', () => {
    const directive = new HigherHiglightsDirective();
    expect(directive).toBeTruthy();
  });
});
