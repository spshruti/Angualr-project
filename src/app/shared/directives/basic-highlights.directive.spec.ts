import { BasicHighlightsDirective } from './basic-highlights.directive';

describe('BasicHighlightsDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicHighlightsDirective();
    expect(directive).toBeTruthy();
  });
});
